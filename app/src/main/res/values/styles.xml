<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:buttonStyle">@style/MyStyleButton</item>
        <item name="android:textColor">@color/text</item>
        <item name="android:alertDialogTheme">@style/MyStyleDialog</item>

    </style>
    <style name="MyStyleButton" parent="Widget.AppCompat.Button.Colored">
        <item name="android:background">@drawable/button</item>
        <item name="android:textColor">@color/textButton</item>
<!--        <item name="android:textColor">@drawable/text_color</item>-->
        <item name="android:padding">10dp</item>
    </style>

    <style name="MyStyleDialog" parent="Base.V7.Theme.AppCompat.Light">
        <item name="android:windowBackground">@color/background</item>
        <item name="android:windowIsFloating">true</item>
    </style>

</resources>